# ReClaw 2.0
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtualenv
.venv/
venv/
ENV/
env/

# Secrets & local config
.env
*.pem
*.key
keys/
secrets/

# Runtime data (but we do want to commit some seeds and examples)
data/runs/
data/sessions/
outputs/
*.log

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Docker
*.pid

# Temp from clawsmith/clawforge runs (per plan.md Phase 1)
generated-*/
generated-*

# Backup safety net (NEVER delete on disk; exclude from git per mission rules)
openclaw-workspace-backup/

# Large data files and junk (>10MB raw data, session notes, images - stay out of git per rules)
gateway_disbursements_2023.txt
openclaw-workspace-backup/**/*.txt
image*.jpg
session-notes-*.md
